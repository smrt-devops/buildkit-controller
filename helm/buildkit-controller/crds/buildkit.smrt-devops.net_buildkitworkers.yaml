---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: buildkitworkers.buildkit.smrt-devops.net
spec:
  group: buildkit.smrt-devops.net
  names:
    kind: BuildKitWorker
    listKind: BuildKitWorkerList
    plural: buildkitworkers
    singular: buildkitworker
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.poolRef.name
      name: Pool
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.podName
      name: Pod
      type: string
    - jsonPath: .spec.allocation.jobId
      name: JobID
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BuildKitWorker is the Schema for the buildkitworkers API.
          Represents a single ephemeral BuildKit worker instance allocated to a job.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BuildKitWorkerSpec defines the desired state of BuildKitWorker.
            properties:
              allocation:
                description: Allocation contains job allocation information (set when
                  allocated)
                properties:
                  allocatedAt:
                    description: AllocatedAt is when the worker was allocated
                    format: date-time
                    type: string
                  expiresAt:
                    description: ExpiresAt is when the allocation expires (optional
                      timeout)
                    format: date-time
                    type: string
                  jobId:
                    description: JobID is the unique identifier for the allocated
                      job
                    type: string
                  metadata:
                    additionalProperties:
                      type: string
                    description: Metadata contains optional job metadata
                    type: object
                  requestedBy:
                    description: RequestedBy is the identity that requested the allocation
                    type: string
                  token:
                    description: Token is the allocation token for authentication
                    type: string
                required:
                - allocatedAt
                - jobId
                - token
                type: object
              poolRef:
                description: PoolRef references the parent BuildKitPool
                properties:
                  name:
                    description: Name is the name of the BuildKitPool
                    type: string
                  namespace:
                    description: Namespace is the namespace of the BuildKitPool (defaults
                      to same namespace)
                    type: string
                required:
                - name
                type: object
            required:
            - poolRef
            type: object
          status:
            description: BuildKitWorkerStatus defines the observed state of BuildKitWorker.
            properties:
              allocationCount:
                description: AllocationCount is the number of times this worker has
                  been allocated
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: CreatedAt is when the worker was created
                format: date-time
                type: string
              endpoint:
                description: |-
                  Endpoint is the internal endpoint for the gateway to reach this worker
                  Format: <pod-ip>:1234 (plain buildkitd port)
                type: string
              lastActivityAt:
                description: LastActivityAt is when the worker last had activity
                format: date-time
                type: string
              message:
                description: Message provides additional status information
                type: string
              phase:
                description: Phase is the current lifecycle phase
                enum:
                - Pending
                - Provisioning
                - Running
                - Idle
                - Allocated
                - Terminating
                - Failed
                type: string
              podIP:
                description: PodIP is the IP address of the worker pod
                type: string
              podName:
                description: PodName is the name of the worker pod
                type: string
              readyAt:
                description: ReadyAt is when the worker became ready
                format: date-time
                type: string
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
