---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: buildkitoidcconfigs.buildkit.smrt-devops.net
spec:
  group: buildkit.smrt-devops.net
  names:
    kind: BuildKitOIDCConfig
    listKind: BuildKitOIDCConfigList
    plural: buildkitoidcconfigs
    singular: buildkitoidcconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.issuer
      name: Issuer
      type: string
    - jsonPath: .spec.audience
      name: Audience
      type: string
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BuildKitOIDCConfig is the Schema for the buildkitoidcconfigs API.
          This is a cluster-scoped resource for configuring OIDC authentication.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BuildKitOIDCConfigSpec defines the desired state of BuildKitOIDCConfig.
            properties:
              audience:
                description: Audience is the expected audience for tokens
                type: string
              claimsMapping:
                description: ClaimsMapping maps OIDC claims to user/pool information
                properties:
                  pools:
                    description: Pools is the claim name for pool access list
                    type: string
                  user:
                    description: User is the claim name for user identity
                    type: string
                type: object
              clientID:
                description: ClientID is the OAuth2 client ID (optional, for token
                  exchange)
                type: string
              clientSecretRef:
                description: ClientSecretRef is a reference to a secret containing
                  the OAuth2 client secret
                properties:
                  key:
                    description: Key is the key in the secret (defaults to "client-secret")
                    type: string
                  name:
                    description: Name is the name of the secret
                    type: string
                  namespace:
                    description: Namespace is the namespace of the secret (defaults
                      to same namespace as config)
                    type: string
                required:
                - name
                type: object
              enabled:
                default: true
                description: Enabled controls whether this OIDC configuration is active
                type: boolean
              issuer:
                description: Issuer is the OIDC issuer URL
                type: string
            required:
            - audience
            - issuer
            type: object
          status:
            description: BuildKitOIDCConfigStatus defines the observed state of BuildKitOIDCConfig.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message is a human-readable status message
                type: string
              ready:
                description: Ready indicates whether the OIDC configuration is ready
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
